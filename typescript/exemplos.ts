let ativo: boolean = true

let estaChovendo: boolean = false

let nome: string = 'Carla'

let idade: number = 23

let numeros: number[] = [1, 2, 3, 4, 5]
let frutas: string[] = ['Maçã', 'Uva', 'Banana']

let conhecimentos: Array<string> = ['Javascript', 'Java', 'Css']

let array: (string | number | boolean)[] = [1, 2, 3, 'Laranja', true]

let valorQualquer: any = 42

valorQualquer = 'Carla'

let indefinido: undefined = undefined

let nulo: null = null

let dados: {
  usuario: string,
  idade: number, 
  ativo: boolean,
  conhecimento: string[]
} = {
  usuario: 'João',
  idade: 22,
  ativo: true,
  conhecimento: ['Javascript', 'HTML', 'CSS']
}